#!/bin/sh
set -e

has_updates=0
while read local_ref local_sha remote_ref remote_sha
do
	if [ "$local_sha" != "$remote_sha" ]; then
		has_updates=1
		break
	fi
done

[ "$has_updates" -eq 1 ] || exit 0

bun scripts/bump-release-on-push.mjs
