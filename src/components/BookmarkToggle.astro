---
const { id, title, description = "", date = "", href, variant = "article" } = Astro.props;
const isCard = variant === "card";
---

<button
	type="button"
	class={`bookmark-toggle border px-3 py-1.5 text-sm font-semibold transition border-[color:var(--border)] bg-transparent text-[color:var(--text)] hover:border-[color:var(--accent)] hover:text-[color:var(--accent)] ${isCard ? "relative z-10" : ""}`}
	data-bookmark-toggle
	data-bookmark-id={id}
	data-bookmark-title={title}
	data-bookmark-description={description}
	data-bookmark-date={date}
	data-bookmark-href={href}
	aria-pressed="false"
>
	В закладки
</button>
