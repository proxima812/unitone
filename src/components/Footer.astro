---
import { links, telegrams } from "@/data/links"
import { methodsByTitle } from "@/data/methods"
import Container from "./Container.astro"

const technologies = [
	{ label: "astrojs", href: "https://astro.build/" },
	{ label: "tailwind", href: "https://tailwindcss.com/" },
	{ label: "supabase", href: "https://supabase.com/" },
	{ label: "typescript", href: "https://www.typescriptlang.org/" },
	{ label: "vercel", href: "https://vercel.com/" },
	{ label: "react", href: "https://react.dev/" },
	{ label: "PWA", href: "https://web.dev/explore/progressive-web-apps" },
	{ label: "mdx", href: "https://mdxjs.com/" },
	{ label: "swr", href: "https://swr.vercel.app/" },
]
---

<footer class="border-t border-[color:var(--border)] bg-[color:var(--surface)]/92 backdrop-blur">
	<Container>
		<div class="grid grid-cols-1 gap-12 py-12 md:grid-cols-2 lg:grid-cols-4">
			<div class="flex flex-col items-center gap-4 md:items-start">
				<span class="text-lg font-semibold text-[color:var(--text)]">Страницы</span>
				{
					links &&
						links.map((item) => (
							<a class="text-[color:var(--muted)] hover:text-[color:var(--accent)]" href={item.href}>
								<span>{item.label}</span>
							</a>
						))
				}
			</div>
			<div class="flex flex-col items-center gap-4 md:items-start">
				<span class="text-lg font-semibold text-[color:var(--text)]">Методы шагов</span>
				{
					methodsByTitle &&
						methodsByTitle.map((item) => (
							<a class="text-[color:var(--muted)] hover:text-[color:var(--accent)]" href="/methods">
								{item.title}
							</a>
						))
				}
			</div>
			<div class="flex flex-col items-center gap-4 md:items-start">
				<span class="text-lg font-semibold text-[color:var(--text)]">Ресурсы</span>
				{
					telegrams &&
						telegrams.map((item) => (
							<a class="text-[color:var(--muted)] hover:text-[color:var(--accent)]" target="_blank" href={item.href}>
								{item.label}
							</a>
						))
				}
			</div>
			<div class="flex flex-col items-center gap-4 md:items-start">
				<span class="text-lg font-semibold text-[color:var(--text)]">Информация</span>
				<a class="text-[color:var(--muted)] hover:text-[color:var(--accent)]" href="/about">О проекте</a>
				<a class="text-[color:var(--muted)] hover:text-[color:var(--accent)]" href="/privacy">Политика конфиденциальности</a>
				<a class="text-[color:var(--muted)] hover:text-[color:var(--accent)]" href="/terms">Условия использования</a>
				<a class="text-[color:var(--muted)] hover:text-[color:var(--accent)]" href="/disclaimer">Дисклеймер</a>
				<a class="text-[color:var(--muted)] hover:text-[color:var(--accent)]" href="/editorial-policy">Ред. политика</a>
			</div>
		</div>

		<div class="my-12 rounded-2xl border border-[color:var(--border)] bg-[color:var(--surface)] p-6">
			<div class="flex flex-wrap items-center justify-center gap-3 md:justify-between md:flex-nowrap">
				<p>
					&copy;
					<span class="font-medium text-[color:var(--muted)]">{new Date().getFullYear()}</span>
					<span class="font-bold">Unity One</span>
				</p>
				<p class="text-[color:var(--muted)]">сделано с душой и вниманием 💙</p>
			</div>

			<p class="mt-4 text-sm text-[color:var(--muted)]">
				Технологии проекта:
				{
					technologies.map((tech, index) => (
						<>
							{index > 0 && ", "}
							<a
								class="underline underline-offset-4 hover:text-[color:var(--accent)]"
								href={tech.href}
								target="_blank"
								rel="noopener noreferrer"
							>
								{tech.label}
							</a>
						</>
					))
				}
			</p>
			<p class="mt-4 text-sm text-[color:var(--muted)]">
				Источники: открытые материалы, авторские мнения и личный опыт участников.
			</p>
			<p class="mt-2 text-sm text-[color:var(--muted)]">
				Иногда встречается шуточный контент - относитесь к нему с легкостью.
			</p>
		</div>
	</Container>
</footer>
